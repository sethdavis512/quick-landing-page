{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": ["DATABASE_URL", "PORT"]
    },
    "dev": {
      "env": ["DATABASE_URL", "PORT"]
    }
  }
}
