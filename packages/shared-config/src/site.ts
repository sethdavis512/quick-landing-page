export interface SiteConfig {
  title: string;
  description: string;
  lang: string;
}

export const siteConfig: SiteConfig = {
  title: "Quick Landing Page",
  description: "A fast, modern landing page",
  lang: "en",
};
